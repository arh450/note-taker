const router = require("express").Router();

// allow users to create and save notes (POST)

// allow users to view previous saved notes (GET)

// allow users to delete previous saved notes


module.exports = router;